PORT=3001
HOST=127.0.0.1
CORS_ORIGIN=http://localhost:8080,http://127.0.0.1:8080
PYTHON_BIN=./venv/bin/python

# Optional Hugging Face cache/token settings for local model setup.
# The app should run offline after required models are already cached locally.
HF_HOME=./models/huggingface
HF_TOKEN=
